apiVersion: v1
kind: Service
metadata:
  name: nginx

spec:
  selector:
    project: roboshop
    environment: prod
    component: frontend

  ports:
  - protocol: TCP
    port: 37800
    targetPort: 80


  
